{%- macro appbox(title, description=null, icon=null, url=null, type=null) -%}
  {%- set content -%}
    {%- if icon -%}
    <img src="{{ icon }}" class="app-icon" alt aria-hidden>
    {%- endif -%}
    <div class="app-info">
      {%- if type == "title" -%}
      <h1 class="app-title">{{ title }}</h1>
      {%- else -%}
      <b class="app-title">{{ title }}</b>
      {%- endif -%}
      {%- if description -%}
      <div class="app-desc">{{ description }}</div>
      {%- endif -%}
    </div>
  {%- endset -%}
  {%- if url -%}
  <a href="{{ url }}" class="app">
    {{ content | safe }}
  </a>
  {%- else -%}
  <div class="app">
    {{ content | safe }}
  </div>
  {%- endif -%}
{%- endmacro -%}